解释器模式
概念：定义一个语言的文法，并且建立一个解释器来解释该语言中的句子，这里的“语言”是指使用规定格式和语法的代码。

-- code --
        Expression aExpression = new AExpression();
        Expression bExpression = new BExpression();
        Target target = new Target();
        aExpression.interpret(target);
        bExpression.interpret(target);